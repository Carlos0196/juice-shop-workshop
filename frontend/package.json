{
  "name": "frontend",
  "version": "16.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "lint:scss": "npx stylelint \"**/*.scss\"",
    "postinstall": "ngcc --tsconfig \"./src/tsconfig.app.json\""
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
